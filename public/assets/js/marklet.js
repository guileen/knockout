(function() {
// ---
if (window.__IMGKO_MARKLET_LOADED__) return;
window.__IMGKO_MARKLET_LOADED__ = true;
// ---
var overlay = createOverlay();
var container = createUIContainer();

// template.js
//
var html = template.render();
$(img).each(function());
container.append($(html));

// ---
})();
